function getScore(distance, cost_ratings, reviews){

}

export default function compute(facilities){
    const scores = [];
    for(const i of facilities){
        
        scores.push(getScore(i.distance, i.cost_ratings, ))
    }
}